# -*- mode: snippet -*-
# name: vows
# --
var vows   = require('vows'),
    assert = require('assert');

vows.describe('$1')
  .addBatch({
    '$2': {
      topic: $0
      }
    })
  .export(module);
