kind=defined
names=assert_in_delta
visibility=public

--- assert_in_delta(expected_float, actual_float, delta, message="")    -> ()

(expected_float.to_f - actual_float.to_f).abs <= delta.to_f 
が真ならパスします。

delta は正の数でなければならない。

@raise Test::Unit::AssertionFailedError assert が失敗した時に発生します。

