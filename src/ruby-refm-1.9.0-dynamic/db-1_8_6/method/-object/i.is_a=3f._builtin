visibility=public
kind=defined
names=is_a?,kind_of?

--- is_a?(klass_or_mod) -> bool
--- kind_of?(klass_or_mod) -> bool

オブジェクトがクラス mod とそのサブクラスまたは
モジュール mod をインクルードしたクラスとそのサブクラス
のいずれかのインスタンスであるとき真を返します。

@param klass_or_mod クラスやモジュールなど、[[c:Module]]かそのサブクラスのインスタンスです。

    module M
    end
    class C < Object
      include M
    end
    class S < C
    end

    obj = S.new
    p obj.is_a?(S)       # true
    p obj.is_a?(C)       # true
    p obj.is_a?(Object)  # true
    p obj.is_a?(M)       # true
    p obj.is_a?(Hash)    # false

@see [[m:Object#instance_of?]],[[m:Module#===]],[[m:Object#class]]

