visibility=public
kind=defined
names=def_delegators,def_singleton_delegators

--- def_singleton_delegators(accessor, *methods)
--- def_delegators(accessor, *methods)

methods で渡されたメソッドのリストを accessor に委譲する
ようにします。

