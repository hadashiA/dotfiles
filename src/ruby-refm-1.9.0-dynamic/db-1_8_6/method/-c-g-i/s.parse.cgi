visibility=public
kind=defined
names=parse

--- parse(query)

QUERY_STRING をパースします。

        例：
        params = CGI.parse("query_string")
          # {"name1" => ["value1", "value2", ...],
          #  "name2" => ["value1", "value2", ...], ... }

