visibility=public
kind=defined
names=drop

--- drop(n)               -> Array

Drops first n elements from enum, and returns rest elements in
an array.

   a = [1, 2, 3, 4, 5, 0]
   a.drop(3)             # => [4, 5, 0]


