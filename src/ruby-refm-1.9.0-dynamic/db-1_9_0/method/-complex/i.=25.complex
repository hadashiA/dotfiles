visibility=public
kind=defined
names=%

--- %(c)
実部同士の演算結果 m1 と虚部同士の演算結果 m2 を複素数
Complex.new(m1, m2)にして返します。つまり、

  z1 = a + b * i
  z2 = c + d * i

としたとき、計算結果は

  z1 % z2 = (a % c) + (b % d) * i

です。

