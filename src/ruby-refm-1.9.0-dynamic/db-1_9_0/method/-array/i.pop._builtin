visibility=public
kind=defined
names=pop

--- pop    -> object | nil
--- pop(n)    -> Array

末尾の要素を取り除いてそれを返します。自身が空配列の時は
nil を返します。

      array = [1, [2, 3], 4]
      p array.pop      # => 4
      p array.pop      # => [2, 3]
      p array          # => [1]

      p array.pop      # => 1
      p array.pop      # => nil
      p array          # => []

@see [[m:Array#push]], [[m:Array#shift]], [[m:Array#unshift]] 

