kind=defined
names=index,key
visibility=public

--- index(val) -> String
--- key(val)   -> String

このメソッドは obsolete です。
使用すると警告メッセージが表示されます。

val に対応するキーを返します。対応する要素が存在しない時には
nil を返します。

@param val  値を指定します。 to_str で文字列化されます。

